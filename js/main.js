// Main application initialization
document.addEventListener('DOMContentLoaded', () => {
    // Initialize all modules
    State.initialize();
    UI.initialize();
    DragDrop.initialize();
    Modals.initialize();
});